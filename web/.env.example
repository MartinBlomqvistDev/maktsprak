# Supabase — read-only, same project as the Python ETL pipeline.
# Get these from Supabase → Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# FastAPI inference service (Phase 2 — see /metod). Leave unset locally;
# /api/predict returns a graceful "not connected" response until this exists.
INFERENCE_SERVICE_URL=
